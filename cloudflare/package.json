{
  "name": "cf-d1-timecapsule",
  "version": "1.0.0",
  "private": true,
  "devDependencies": {
    "wrangler": "^3.78.0"
  },
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "migrate:local": "wrangler d1 execute timecapsule --local --file=./schema.sql",
    "migrate:prod": "wrangler d1 execute timecapsule --file=./schema.sql --remote"
  }
}
